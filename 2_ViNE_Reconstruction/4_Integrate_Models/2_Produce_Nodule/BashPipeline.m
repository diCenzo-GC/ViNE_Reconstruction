%% Launch bash pipeline

%%%%%%% now shift to ShareMetsPipeline.sh %%%%%%%%%%%%%%%%%% now shift to
    %%%%%%% ShareMetsPipeline.sh %%%%%%%%%%%%%%%%%% now shift to
    %%%%%%% ShareMetsPipeline.sh %%%%%%%%%%%


     pathToScript = fullfile(pwd,'ShareMetsPipeline.sh');  % assumes script is in curent directory
     cmdStr       = [pathToScript];
     system(cmdStr);
